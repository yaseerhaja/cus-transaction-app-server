{
  "name": "express-server",
  "version": "0.0.1",
  "license": "",
  "scripts": {
    "start:mac": "tsc && cp transactions.json dist/transactions.json && node dist/server.js",
    "start:windows": "tsc && copy transactions.json dist\\transactions.json && node dist\\server.js",
    "build": "tsc && cp transactions.json dist/",
    "build:windows": "tsc && copy transactions.json dist\\",
    "test": "echo 'No tests specified' && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-graphql": "^0.12.0",
    "graphql": "^15.8.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "18.11.9",
    "typescript": "4.8.4"
  }
}
